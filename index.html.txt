<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ZENO | TITAN OPERATING SYSTEM v2.0</title>
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:ital,wght@0,700;1,900&family=Inter:wght@300;400;700;900&display=swap" rel="stylesheet">
    <style>
        :root {
            --bg: #050505;
            --card: rgba(15, 15, 15, 0.95);
            --accent: #d4af37; 
            --accent-glow: rgba(212, 175, 55, 0.4);
            --text-main: #ffffff;
            --text-dim: #888888;
            --titan-gradient: linear-gradient(135deg, #d4af37 0%, #f2d06b 50%, #b8860b 100%);
        }

        body, html {
            margin: 0;
            padding: 0;
            background-color: var(--bg);
            color: var(--text-main);
            font-family: 'Inter', sans-serif;
            overflow-x: hidden;
            height: 100%;
        }

        /* Ambient Background Pulse */
        body::before {
            content: "";
            position: fixed;
            top: 50%;
            left: 50%;
            width: 80vw;
            height: 80vw;
            background: radial-gradient(circle, rgba(212, 175, 55, 0.03) 0%, transparent 70%);
            transform: translate(-50%, -50%);
            z-index: -1;
            animation: pulse 8s infinite ease-in-out;
        }

        @keyframes pulse { 0%, 100% { opacity: 0.5; transform: translate(-50%, -50%) scale(1); } 50% { opacity: 1; transform: translate(-50%, -50%) scale(1.1); } }

        nav {
            padding: 40px 10%;
            display: flex;
            justify-content: space-between;
            align-items: center;
            border-bottom: 1px solid rgba(212, 175, 55, 0.1);
        }

        .logo {
            font-family: 'Playfair Display', serif;
            font-size: 2rem;
            letter-spacing: 20px;
            color: var(--accent);
            text-transform: uppercase;
            font-weight: 900;
        }

        .main-container {
            max-width: 900px;
            margin: 80px auto;
            padding: 0 20px;
            text-align: center;
        }

        .titan-card {
            background: var(--card);
            border-radius: 40px;
            padding: 80px 60px;
            border: 1px solid rgba(212, 175, 55, 0.2);
            box-shadow: 0 50px 100px rgba(0,0,0,0.8), 0 0 40px rgba(212, 175, 55, 0.05);
            backdrop-filter: blur(10px);
        }

        h1 {
            font-family: 'Playfair Display', serif;
            font-size: 4rem;
            margin-bottom: 20px;
            background: var(--titan-gradient);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            letter-spacing: -2px;
        }

        textarea {
            width: 100%;
            background: rgba(0,0,0,0.5);
            border: 1px solid rgba(212, 175, 55, 0.2);
            color: #fff;
            padding: 30px;
            margin: 40px 0;
            border-radius: 20px;
            font-size: 1.3rem;
            box-sizing: border-box;
            transition: 0.3s;
            font-family: 'Inter';
        }

        textarea:focus { outline: none; border-color: var(--accent); box-shadow: 0 0 20px var(--accent-glow); }

        .titan-btn {
            background: var(--titan-gradient);
            padding: 25px 60px;
            border-radius: 50px;
            border: none;
            color: #000;
            font-weight: 900;
            font-size: 1rem;
            letter-spacing: 5px;
            text-transform: uppercase;
            cursor: pointer;
            transition: 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
        }

        .titan-btn:hover { transform: scale(1.05) translateY(-5px); box-shadow: 0 15px 30px rgba(212, 175, 55, 0.3); }

        /* Pop-up (Modal) Styles */
        .modal {
            display: none;
            position: fixed;
            top: 0; left: 0; width: 100%; height: 100%;
            background: rgba(0,0,0,0.95);
            z-index: 1000;
            justify-content: center;
            align-items: center;
            animation: fadeIn 0.5s ease;
        }

        .modal-content {
            width: 80%;
            max-width: 600px;
            text-align: center;
            padding: 60px;
            border: 1px solid var(--accent);
            border-radius: 40px;
            background: #0a0a0a;
        }

        @keyframes fadeIn { from { opacity: 0; } to { opacity: 1; } }

        .typewriter {
            font-family: 'Playfair Display', serif;
            font-size: 2.5rem;
            line-height: 1.3;
            margin-bottom: 30px;
            color: var(--text-main);
        }

        .annotation-box {
            font-style: italic;
            color: var(--accent);
            font-size: 1.1rem;
            line-height: 1.8;
            border-top: 1px solid rgba(212, 175, 55, 0.2);
            padding-top: 20px;
        }
    </style>
</head>
<body>

<nav>
    <div class="logo">ZENO</div>
    <div id="stat-count" style="font-weight: 900; color: var(--accent); letter-spacing: 3px; font-size: 0.8rem;">OS STATUS: ACTIVE</div>
</nav>

<div class="main-container">
    <div class="titan-card">
        <label style="color: var(--accent); letter-spacing: 10px; font-weight: 900; font-size: 0.7rem;">SYSTEM.INITIALIZE</label>
        <h1>Logic Over Emotion.</h1>
        <p style="color: var(--text-dim); letter-spacing: 2px;">Input the chaos. Receive the order.</p>
        
        <textarea id="userInput" rows="4" placeholder="What disturbs your internal peace?"></textarea>
        
        <button class="titan-btn" onclick="processLogic()">Submit to Reason</button>
    </div>
</div>

<div id="wisdomModal" class="modal">
    <div class="modal-content">
        <div id="quoteOutput" class="typewriter"></div>
        <div id="authorOutput" style="letter-spacing: 8px; font-weight: 900; color: var(--accent); margin-bottom: 30px;"></div>
        <div id="noteOutput" class="annotation-box"></div>
        <button class="titan-btn" style="margin-top: 40px; font-size: 0.7rem; padding: 15px 30px;" onclick="closeTitan()">Return